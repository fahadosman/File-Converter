# Used by the Worker for POST /api/payments/paddle/verify only (never put API keys in the frontend).
PADDLE_API_KEY=your_paddle_api_key_here
PADDLE_PRICE_ID=pri_xxxxxxxxxxxxxxxxxxxxx
PADDLE_ENV=sandbox

# Checkout: create a client-side token in Paddle → Developer tools → Authentication and set in HTML:
#   window.__PADDLE_CLIENT_TOKEN__ = "test_...";
